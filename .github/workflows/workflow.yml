name: Version Updater

on:
  workflow_dispatch:
    inputs:
      build-version:
        description: 'Build version'
        required: true
        type: string
      repo-directory-path:
        description: 'Directory path'
        required: true
        type: string
      repo-file-name:
        description: 'File name'
        required: true
        type: string
      target-repo:
        description: 'Target repository'
        required: true
        type: string

jobs:
  update-config:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.target-repo }}
          token: ${{ secrets.GH_BOT_TOKEN }}
          path: ${{ inputs.repo-directory-path }}
          ref: master

      - name: Update YAML value
        env:
          VERSION: ${{ inputs.build-version }}
          REPO_DIR: ${{ inputs.repo-directory-path }}
          FILE_PATH: ${{ inputs.repo-file-name }}
        run: |
          cd "$REPO_DIR"

          python3 -m pip install pyyaml

          python3 <<'EOF'
          import os
          import yaml

          file_path = os.environ["FILE_PATH"]
          new_version = os.environ["VERSION"]

          with open(file_path, "r") as f:
              data = yaml.safe_load(f)

          data["app"]["version"] = new_version

          with open(file_path, "w") as f:
              yaml.safe_dump(data, f, sort_keys=False)
          EOF

      - name: Commit and push to target repo
        env:
          REPO_DIR: ${{ inputs.repo-directory-path }}
          FILE_PATH: ${{ inputs.repo-file-name }}
        run: |
          cd "$REPO_DIR"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add "$FILE_PATH"

          if ! git diff --cached --quiet; then
            git commit -m "${{ github.event.repository.name }} (run #${{ github.run_number }}): Update app version to ${{ inputs.build-version }}"
            git push
          else
            echo "::error::No changes to commit"
            exit 1
          fi